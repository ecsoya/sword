
# 代码生成
gen: 
  # 作者
  author: Jin Liu (angryred@qq.com)
  # 默认生成包路径 system 需改成自己的模块名称 如 system monitor tool
  packageName: com.github.ecsoya.sword
  # 自动去除表前缀，默认是false
  autoRemovePre: true
  # 表前缀（生成类名不会包含表前缀，多个用逗号分隔）
  tablePrefix: t_
  # BaseEntity 包名前缀 com.github.ecsoya.sword(.common.core.domain.BaseEntity)
  domainPackagePrefix: com.github.ecsoya.sword